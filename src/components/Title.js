import React from 'react';

const Title = ({ title }) => {
  return (
    <article>
      <h2 className='underline'>{title}</h2>

      {/* <div ></div> */}
    </article>
  );
};

export default Title;
